<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/styles.css" />
    <title><%= articles.title %> - Handiblog</title>
  </head>
  <body>
    <header class="bg-primary text-white py-3">
      <h1 class="text-center">Handiblog</h1>
    </header>
    <div class="container my-5 text-center">
      <h1 class="display-4 mb-3"><%= articles.title %></h1>
      <h2 class="text-muted mb-4"><%= articles.type %></h2>
      <div class="text-center">
        <img
          src="/img/<%= articles.image %>"
          alt="<%= articles.title %>"
          class="img-fluid mb-4"
        />
      </div>
      <div class="article-content bg-light p-4 rounded w-75 m-auto">
        <%= articles.content %>
      </div>
    </div>
  </body>
</html>
